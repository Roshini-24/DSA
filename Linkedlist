#Display list
class Node:
    def __init__(self,data):
        self.data=data
        self.next=None

def displayList(head):
    l=[]
    while head:
        l.append(head.data)
        head=head.next
    return l
